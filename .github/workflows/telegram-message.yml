on:
  pull_request_target:
    types:
      - closed

jobs:
  if_merged:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    steps:
      - run:
          using: 'node20'
          main: 'bot.js'
#      - run: |
#          echo The PR was merged GG