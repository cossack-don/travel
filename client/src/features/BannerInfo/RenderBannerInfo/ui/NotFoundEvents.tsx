import styled from "./NotFoundEvents.module.scss"

const NotFoundEvents = () => {
	return <div className={styled.wrapper}>В данный момент событий нет, создайте полажуйста событие.</div>
}

export default NotFoundEvents
